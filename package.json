{
  "name": "urgent_qr",
  "version": "1.0.0",
  "description": "stock baster for thai set trade",
  "main": "index.js",
  "scripts": {
    "start:dev": "npm run start --prefix client",
    "fire-host": "npm run build --prefix client ; firebase deploy --only hosting",
    "fire-func": "firebase deploy --only functions",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^7.0.0",
    "firebase-functions": "^2.2.0"
  }
}
